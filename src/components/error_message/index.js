import { errors } from '../../resources/texts';
class ErrorMessage {
    constructor(params) {
    }
}

const template = `
<div>PLACEHOLDER</div>
`;

export default { viewModel: ErrorMessage, template };
